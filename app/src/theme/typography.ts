/** Rajdhani: bold, geometric, corporate-tech display face, matching San's "propaganda / hacking" near-future mood. Loaded in index.html. */
export const fontDisplay = `'Rajdhani', 'Arial Narrow', sans-serif`

/** Work Sans: clean, neutral body face, loaded alongside Rajdhani in index.html. */
export const fontBody = `'Work Sans', 'Segoe UI', sans-serif`

/**
 * The rulebook's two faces, used by every dialog so they read like a page of it: Luckiest Guy for its
 * rounded comic capitals (ComicsTricks in the PDF — section bands and red sub-titles) and Barlow
 * Condensed for its running text (Futura Condensed in the PDF). Loaded in index.html.
 */
export const fontRuleTitle = `'Luckiest Guy', 'Rajdhani', sans-serif`
export const fontRuleBody = `'Barlow Condensed', 'Arial Narrow', sans-serif`
